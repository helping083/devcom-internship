import { IfelseDirective } from './ifelse.directive';

describe('IfelseDirective', () => {
  it('should create an instance', () => {
    const directive = new IfelseDirective();
    expect(directive).toBeTruthy();
  });
});
